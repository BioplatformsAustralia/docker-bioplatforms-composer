#!/bin/sh

## ./develop.sh usage

# Print out the usage doc string from all the tools
BASEDIR=$(dirname "$0")
grep -h "^##" "${BASEDIR}"/* | sort | uniq
cat "${BIOPLATFORMS_COMPOSER_HOME}"/share/README.txt
